global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "node-app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "app-instance-1:3000"
          - "app-instance-2:3000"
